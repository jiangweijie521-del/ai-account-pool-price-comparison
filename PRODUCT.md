# 库存比价台

## 目标

把四家指定店铺的公开商品、价格与库存自动汇总到一个中文页面。默认只展示有货商品，同类按价格从低到高排列，相同标题的跨店商品额外相邻比价。

## 使用者与场景

主要使用者依赖语音输入，精细鼠标和频繁键盘操作不便。页面应在打开后自行更新，让使用者用浏览和一次点击完成判断与购买跳转。

## 功能边界

- 数据源固定为用户给出的四个 `pay.ldxp.cn` 店铺，不接受任意远端 URL。
- 浏览器每 5 分钟自动刷新；大号手动刷新按钮会真实重读上游，并受 30 秒全局冷却和单飞锁保护。
- 默认只看有货，可切换显示缺货；商品按辅助关键词分类。
- 旧数据保留原采集时间并明确标记，不参与最低价评选。
- 手机首屏先展示当前低价，长分类默认展示前 6 件并允许一键展开。
- 同类和同款分组只帮助比价，不推断商品规格、售后或质量等价。
- 商品购买仍在原店铺完成。

## 可访问性

- 中文正文至少 18px，控制高度至少 48px。
- 完整键盘焦点、语义标题、状态播报和减少动态效果路径。
- 手机端重排为纵向票据条，不要求横向拖动表格。
